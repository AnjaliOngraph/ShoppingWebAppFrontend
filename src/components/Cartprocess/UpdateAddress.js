// import { Formik } from "formik";

export default function UpdateAddress(props){
console.log(props,"props");

  // console.log(props.addressDetails,"address from ");
    // try {
    //     console.log("eduthandler called");
    //     const updateAddress = {
    //       name: address.name,
    //       mobileNo: address.mobileNo,
    //       email: address.email,
    //       pinCode: address.pinCode,
    //       address: address.address,
    //       city: address.city,
    //       UserId: localStorage.getItem("userId"),
    //     };
  
    //     const res = await fetch(`/updateAddress/${UserId}`, {
    //       method: "PATCH",
    //       headers: {
    //         "content-Type": "application/json",
    //       },
    //       body: JSON.stringify(updateAddress),
    //     });
    //     const updatedDetails = await res.json();
    //     console.log(updatedDetails, "updatedDetails");
    //   } catch (error) {}
return(
  <h1>
    heyy editer
  </h1>
)
  }